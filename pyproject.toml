[project]
name = "image-morph-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "scikit-image",
    "opencv-python",
    "progress",
    "matplotlib",
    # "PyQt5",
]
